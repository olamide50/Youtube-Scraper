apify~=2.0
httpx[http2]~=0.27
beautifulsoup4~=4.12
lxml~=5.2
python-dotenv~=1.0
# crawlee (pulled in by apify) is incompatible with pydantic>=2.9 due to
# the stricter "cannot specify both default and default_factory" validation
pydantic>=2.0.0,<2.9.0
